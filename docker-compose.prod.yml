services:
  lightagent:
    image: ghcr.io/520pt/jkpb-lightagent:latest
    container_name: lightagent
    restart: unless-stopped
    ports:
      - "9899:9899"
    environment:
      TZ: Asia/Shanghai
      LIGHTAGENT_LANG: auto
      CHANNEL_TYPE: web,wechat_group
      WEB_HOST: 0.0.0.0
      WEB_PORT: "9899"
      WEB_PASSWORD: 520pt
      LIGHTAGENT_PUSH_TOKEN: CHANGE_THIS_LIGHTAGENT_PUSH_TOKEN
      AGENT: "true"
      MODEL: deepseek-v4-flash
      DEEPSEEK_API_KEY: CHANGE_THIS_DEEPSEEK_KEY
      DEEPSEEK_API_BASE: https://api.deepseek.com/v1
      WECHAT_GROUP_ENABLED: "true"
      WECHAT_GROUP_SIDECAR_NODE: node
      WECHAT_GROUP_SIDECAR_MEMORY_PATH: /home/agent/lightagent/wechat_group
    volumes:
      - ./lightagent:/home/agent/lightagent

  duty-reminder:
    image: ghcr.io/520pt/jkpb:latest
    container_name: duty-reminder
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      TZ: Asia/Shanghai
      ENABLE_SCHEDULER: "true"
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: CHANGE_THIS_PASSWORD
      NOTIFICATION_SENDER_TYPE: lightagent
      LIGHTAGENT_NOTIFY_URL: http://lightagent:9899/api/push/send
      LIGHTAGENT_WEB_URL: http://lightagent:9899
      LIGHTAGENT_WEB_PASSWORD: 520pt
      LIGHTAGENT_PUSH_TOKEN: CHANGE_THIS_LIGHTAGENT_PUSH_TOKEN
      LIGHTAGENT_NOTIFY_TARGET: ""
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    depends_on:
      - lightagent
