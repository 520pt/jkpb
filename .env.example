# docker-compose.prod.yml 已经把常用默认值直接写好。
# 生产部署通常只需要改后台账号密码。

ADMIN_USERNAME=520pt
ADMIN_PASSWORD=520pt

# 默认启用内置个人微信群桥，不需要单独部署 LightAgent。
WECHAT_BRIDGE_ENABLED=true

# 微信查询内部 token，默认即可；不要暴露给外部。
DUTY_REMINDER_QUERY_TOKEN=520pt
